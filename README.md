# calender-booking-service
Calendar Booking REST API built with Node.js, Express, Sequelize &amp; PostgreSQL. Prevents meeting conflicts using efficient overlap detection algorithm. Features: clean architecture (routes→controllers→services→models), input validation with DTOs, centralized error handling, database indexing for performance, and comprehensive API documentation.
